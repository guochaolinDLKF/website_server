<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaomayi.system.mapper.RoleMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.xiaomayi.system.entity.Role">
        <id column="id" property="id" />
        <result column="name" property="name" />
        <result column="code" property="code" />
        <result column="note" property="note" />
        <result column="sort" property="sort" />
        <result column="create_user" property="createUser" />
        <result column="create_time" property="createTime" />
        <result column="update_user" property="updateUser" />
        <result column="update_time" property="updateTime" />
        <result column="del_flag" property="delFlag" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, name, code, note, sort, create_user, create_time, update_user, update_time, del_flag
    </sql>

    <!-- 根据用户ID查询角色 -->
    <select id="getRolesByUserId" resultType="com.xiaomayi.system.entity.Role">
        SELECT r.* FROM sys_role AS r
        INNER JOIN sys_user_role AS ur ON r.id=ur.role_id
        WHERE ur.user_id=#{userId} AND r.del_flag=0 AND ur.del_flag=0
        ORDER BY r.sort ASC;
    </select>

</mapper>
