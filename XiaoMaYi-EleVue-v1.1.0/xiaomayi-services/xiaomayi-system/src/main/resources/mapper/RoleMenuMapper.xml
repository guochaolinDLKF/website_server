<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaomayi.system.mapper.RoleMenuMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.xiaomayi.system.entity.RoleMenu">
        <id column="id" property="id" />
        <result column="role_id" property="roleId" />
        <result column="menu_id" property="menuId" />
        <result column="create_user" property="createUser" />
        <result column="create_time" property="createTime" />
        <result column="update_user" property="updateUser" />
        <result column="update_time" property="updateTime" />
        <result column="del_flag" property="delFlag" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, role_id, menu_id, create_user, create_time, update_user, update_time, del_flag
    </sql>

    <!-- 获取租户角色菜单权限列表 -->
    <select id="getTenantMenuList" resultType="com.xiaomayi.system.entity.Menu">
        SELECT m.* FROM sys_role AS r
        INNER JOIN sys_role_menu AS rm ON r.id=rm.role_id
        INNER JOIN sys_menu AS m ON m.id=rm.menu_id
        WHERE r.`code`="ROLE_TENANT" AND r.del_flag=0 AND m.del_flag=0 AND rm.del_flag = 0;
    </select>

</mapper>
